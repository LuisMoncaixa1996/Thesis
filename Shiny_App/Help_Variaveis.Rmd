### **Variables / Predictor Variable**

In this section, the response variable will be selected, as well its associated covariates. The variables will be added to the model after clicking on the respective buttons to save.
The **model response variable** is selected in this box where all the columns of the downloaded dataset are represented. The user must select the column that corresponds to the response variable of the study under analysis, which is added to the model.
